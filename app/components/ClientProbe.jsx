'use client';

export default function ClientProbe() {
  return (
    <div
      style={{
        height: '100%',
        width: '100%',
        background: 'green',
        color: 'white',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: 32,
      }}
    >
      CLIENT BOUNDARY OK
    </div>
  );
}
